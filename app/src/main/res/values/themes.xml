<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash Screen Theme -->
    <style name="Theme.splashscreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="postSplashScreenTheme">@style/Theme.Libri</item>
    </style>

    <!--
        Main App Theme.
        Uses android:Theme.Material.Light.NoActionBar as base, with explicit edge-to-edge flags
        to ensure the splash screen's postSplashScreenTheme transition does not reset
        decorFitsSystemWindows. Compose + enableEdgeToEdge() handles the rest at runtime.
    -->
    <style name="Theme.Libri" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>

    <!-- Legacy theme name for compatibility -->
    <style name="Theme.Learncompose" parent="Theme.Libri" />
</resources>